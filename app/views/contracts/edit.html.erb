<div class="page-header">
  <div>
    <h1>Edit Contract</h1>
    <p class="subtle"><%= @contract.contract_code %></p>
  </div>

  <div class="actions">
    <%= link_to "Back", contract_path(@contract), class: "btn" %>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", contract: @contract %>
  </div>
</div>
