<div class="auth-shell">
  <div class="auth-card">
    <div class="auth-header">
      <h1>Import costs</h1>
      <p class="subtle">Upload the Costs template (.xlsx). Costs can be weekly or monthly.</p>
    </div>

    <%= form_with url: contract_cost_imports_path(@contract), local: true, multipart: true, class: "form" do |f| %>
      <div class="field">
        <%= f.label :file, "Excel file" %>
        <%= f.file_field :file, class: "input", accept: ".xlsx" %>
        <p class="hint">Tip: period_type must be week or month.</p>
      </div>

      <div class="form-actions">
        <%= f.submit "Import", class: "btn btn-primary btn-block" %>
        <%= link_to "Cancel", contract_path(@contract), class: "btn btn-block" %>
      </div>
    <% end %>
  </div>
</div>
